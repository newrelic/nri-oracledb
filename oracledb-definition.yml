name: com.newrelic.oracledb
description: Reports status and metrics for oracledb service
protocol_version: 1
os: linux

commands:
  all_data:
    command:
      - ./bin/nr-oracledb
    interval: 15
    prefix: config/kafka